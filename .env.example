# Optional: lifts unauthenticated rate limit (60/hr -> 5000/hr) when fetching GitHub repos.
# Create a fine-grained token with only "Public repositories: Read" at https://github.com/settings/tokens
GITHUB_TOKEN=
